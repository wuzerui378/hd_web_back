package org.area.dao;

import org.area.model.RegionClusterSubmit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegionClusterSubmitRepository extends JpaRepository<RegionClusterSubmit, Integer> {
}
